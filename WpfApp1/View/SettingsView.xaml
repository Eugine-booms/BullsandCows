<UserControl x:Class="BullsAndCowsWPF.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="650">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Настройки игры" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Длина числа:" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NumberLength}" Margin="0,5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Мин. цифра:" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MinDigit}" Margin="0,5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Макс. цифра:" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding MaxDigit}" Margin="0,5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Макс. попыток (0=∞):" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding MaxAttempts}" Margin="0,5"/>

        <CheckBox Grid.Row="5" Grid.ColumnSpan="2" Content="Разрешить повторяющиеся цифры" 
                  IsChecked="{Binding AllowDuplicates}" Margin="0,10"/>

        <Button Grid.Row="6" Grid.ColumnSpan="2" Content="Применить настройки" 
                Command="{Binding ApplySettingsCommand}" HorizontalAlignment="Center" Margin="0,10" Width="200"/>
    </Grid>
</UserControl>
